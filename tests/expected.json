{
  "_comment": "Findings each case file SHOULD produce, worked out by reading finder.py's rules against the case file, not by running finder.py. A file that should stay silent gets an empty list. Matching in run_tests.py is on (line, pattern) only; conf here is used only to split precision/recall by confidence tier.",

  "_known_gaps_comment": "Real leaks that are out of scope for this version - the findings listed are what a detector covering them would report. They are excluded from precision and recall and printed as their own section, so the recall number above them is never read as 'catches everything'. See README.md and notes.txt 2026/09/12.",

  "_known_gaps": {
    "gap_full_series_mean.py": [
      {"line": 6, "pattern": "mean", "conf": "low"}
    ],
    "gap_full_series_std.py": [
      {"line": 5, "pattern": "std", "conf": "low"}
    ],
    "gap_full_series_max.py": [
      {"line": 5, "pattern": "max", "conf": "low"}
    ],
    "gap_full_series_sum.py": [
      {"line": 5, "pattern": "sum", "conf": "low"}
    ],
    "gap_full_series_compare.py": [
      {"line": 10, "pattern": "mean", "conf": "low"}
    ]
  },

  "leak_bfill.py": [
    {"line": 4, "pattern": "bfill", "conf": "high"}
  ],
  "leak_backfill_method.py": [
    {"line": 4, "pattern": "backfill", "conf": "high"}
  ],
  "leak_shift_negative.py": [
    {"line": 4, "pattern": "shift", "conf": "high"}
  ],
  "leak_shift_negative_periods_kwarg.py": [
    {"line": 4, "pattern": "shift", "conf": "high"}
  ],
  "leak_shift_large_negative.py": [
    {"line": 4, "pattern": "shift", "conf": "high"}
  ],
  "leak_shift_negative_via_constant.py": [
    {"line": 5, "pattern": "shift", "conf": "high"}
  ],
  "leak_rolling_center.py": [
    {"line": 4, "pattern": "rolling", "conf": "high"}
  ],
  "leak_rolling_center_short_shift.py": [
    {"line": 7, "pattern": "rolling", "conf": "high"}
  ],
  "leak_normalize_inline.py": [
    {"line": 9, "pattern": "min", "conf": "low"},
    {"line": 9, "pattern": "max", "conf": "low"}
  ],
  "leak_normalize_helper.py": [
    {"line": 10, "pattern": "min", "conf": "low"},
    {"line": 10, "pattern": "max", "conf": "low"}
  ],
  "leak_normalize_threshold.py": [
    {"line": 8, "pattern": "mean", "conf": "low"}
  ],
  "leak_fill_value_mean.py": [
    {"line": 8, "pattern": "mean", "conf": "low"}
  ],
  "leak_mixed_window_scales.py": [
    {"line": 9, "pattern": "mean", "conf": "low"}
  ],

  "clean_shift_positive.py": [],
  "clean_shift_positive_periods_kwarg.py": [],
  "clean_shift_bare.py": [],
  "clean_shift_positive_via_constant.py": [],
  "clean_shift_reassigned_unknown.py": [],
  "clean_rolling_mean.py": [],
  "clean_rolling_center_false.py": [],
  "clean_rolling_center_compensated.py": [],
  "clean_ffill_not_bfill.py": [],
  "clean_aggregate_after_rolling.py": [],
  "clean_aggregate_after_expanding.py": [],
  "clean_aggregate_after_ewm.py": [],
  "clean_aggregate_after_groupby.py": [],
  "clean_aggregate_axis_one.py": [],
  "clean_normalize_bounded_slice.py": [],
  "clean_ignore_comment.py": [],

  "fp_posthoc_sharpe.py": [],
  "fp_manual_window.py": [],
  "fp_aggregate_bounded_by_slicing.py": [],
  "fp_shift_bounded_slice.py": [],
  "fp_aggregate_in_plot_call.py": [],
  "fp_aggregate_never_decided_on.py": [],
  "fp_sum_in_sibling_lambda.py": [],
  "fp_broadcast_reduced_to_scalar.py": []
}
